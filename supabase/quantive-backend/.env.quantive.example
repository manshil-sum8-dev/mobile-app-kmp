# Copy to .env.quantive and set values. This file is safe to commit; .env.quantive should be gitignored.
# The anon key will be generated by Supabase when the project starts. For local dev, you can leave placeholders.

SUPABASE_ANON_KEY=replace-with-local-anon-key
SUPABASE_SERVICE_ROLE_KEY=replace-with-local-service-role-key
# If you run on a device, use your host machine LAN IP (e.g., http://192.168.1.10:54322)
SUPABASE_URL=http://127.0.0.1:54323
